import React from 'react';

const Header = () => {
    return(
        <nav className="navbar navbar-dark bg-dark row">
            <a className="navbar-brand" href="#index.html">OpenDota</a>
        </nav>

)}

export default Header